
critical: CLAUDE.md and PROGRESS.md have been updated with respect to a new github protocol. please understand those changes, and implement their requirements.

i originally proposed adding the district block assembly directly to the main screen, thinking that it could sit in a fixed position. however, that design idea stopped working when it became clear just how much space this feature would take up. it clearly deserves its own space to live in, but also have a way of bringing it into the map. therefore, i propose the following for implementation (use district_arrangement_US_shape.json district block assembly layout data, but first move the file into a location consistent with the data categorization standards in this project):
supreme court/scotus tab in the selector bar should be changed to a "Summary" tab, which should have a larger-than-usual selector button set to the first option by default [ SCOTUS | Appellate | District ]:
    a. then when the selector is set to SCOTUS, that is where the supreme court view is moved. however, we will keep it in a Majority-like state, by eliminating Timeline and Change for this Summary tab. additionally, i would like the Majority-view hemicircle to be split up into two, with an outer ring of 6 and an inner ring of 3 (that inner ring should have its first and last icons up by 15 deg from the horizontal 0 deg/180 deg), order-filled with the same algorithm as usual. also, its the icons should be doubled in size, since it is SCOTUS.
    b. for now, we'll leave the Appellate section blank and come back to it later.
    c. when set to District, the space should show the assembled liftable cartogram, with district court-square synced block growing on-hover. liftable means that upon pressing a button "Set upon map" (which, while the assembly is deployed and viewing this page, should switch to "Remove from map", and do just that undoing action a later button 1 is referenced to do), the summary pane flips up but allows the whole of the district court block US-resembling assembly to stay, then the blocks move and shrink into a default position on the map. when the map appears, for its view area, there should be standard buttons styled as fill color-transparent but dark circle and button label colors for [1] closing/returning the district blocks to the summary, that when clicked, turns into [2] a button to bring back the district blocks without navigating to that menu. then another two buttons [3+4] which are +/- buttons to shrink/grow the district blocks to a custom size, these of which hide when button 1 is clicked. while the "pull out of one interface and into another" effect should exist for deploying the district assembly, it should not exist when returning it, and the drop-down pane should cover it by default along with the rest of the map view. the *full* district assembly should not follow the map view when it goes into a district drill-in map view, except for a district subassembly belonging to the drilled-in circuit which should be present in a fixed position regardless of the nationwide district assembly deployment (excluding the DC and Federal circuits). in summary, the assembly can be disengaged from the pop down window, and then continue to be usable on the map window according to resizing, cursor-click-drag to reposition, and on-hover also highlighting in standard blue the district court it corresponds to on the geographic map. the main features of the full district assembly that do not exist when set upon the map, which are cut off from the assembly when "Set upon map" is pressed (they still exist for a "replacement" assembly, showing up on its next re-open, in Summary > District even while the other is deployed), and should only exist in the Summary > District pane, is (1) the functionality of clicking a block belonging to a district court, to pin its display information (like how judge icon details get pinned to the docked detail viewer) into the next feature, (2) a docked detail viewer on the right of the assembly, which includes a button to jump to that district court's pop down window from the drill-in view's selector bar.









